import { attributeValue } from '../../../helpers/attribute-value';
import { module, test } from 'qunit';

module('Unit | Helper | attribute value');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = attributeValue(42);
  assert.ok(result);
});
