import { responsibility } from '../../../helpers/responsibility';
import { module, test } from 'qunit';

module('Unit | Helper | responsibility');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = responsibility(42);
  assert.ok(result);
});
